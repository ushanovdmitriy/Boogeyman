"""
Анализ истории чата.

Модуль выполняет два вида анализа:
1. Локальная статистика — подсчёт сообщений, активных участников, топ-слов и т.д.
   Делается без обращения к внешним API.
2. AI-анализ через Anthropic API — отправляет сводку истории чата в Claude
   и получает текстовый инсайт: основные темы, тональность, ключевые события.

Результат — словарь со всеми собранными данными, передаётся в renderer.py.
"""

# TODO: реализовать функцию compute_local_stats(messages: list[dict]) -> dict
#   - Общее количество сообщений
#   - Количество уникальных участников
#   - Топ-5 участников по количеству сообщений
#   - Распределение сообщений по дням (для графика активности)
#   - Топ-20 слов (без стоп-слов)
#   - Количество медиа-сообщений по типу

# TODO: реализовать функцию analyze_with_ai(messages: list[dict]) -> str
#   - Создать краткую текстовую сводку чата (не передавать весь текст из-за лимитов)
#   - Отправить запрос в Anthropic API через клиент из config.py
#   - Промпт: выявить основные темы обсуждений, тональность, необычные события
#   - Вернуть текстовый ответ Claude

# TODO: реализовать функцию run_analysis(messages: list[dict]) -> dict
#   - Объединить результаты compute_local_stats и analyze_with_ai
#   - Добавить метаданные: период анализа, дату генерации отчёта
